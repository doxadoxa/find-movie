import { posterBg } from '../../../helpers/poster-bg';
import { module, test } from 'qunit';

module('Unit | Helper | poster bg');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = posterBg(42);
  assert.ok(result);
});
